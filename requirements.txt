# Backend
Django>=4.2                         # Main backend web framework
djangorestframework>=3.14           # Builds REST APIs for frontend (Student / Faculty / Admin dashboards)

# Authentication
djangorestframework-simplejwt>=5.3  # secure login sessions and role-based access
bcrypt>=4.0                         # password hashing for user credentials

# Database
mysqlclient>=2.2                    # MySQL database connector for Django (fast and stable)

# File Uploads & Data Handling
pandas>=2.0                         # Reads Excel / CSV files (grade upload, attendance upload)
openpyxl>=3.1                       # Required for handling Excel (.xlsx) files

# CORS & API Access
django-cors-headers>=4.3            # Allows frontend (React/Vue) to safely access backend APIs

# Monitoring
django-filter>=23.5                 # Enables filtering in APIs (attendance, grades, reports)

# Testing
pytest>=7.4                         # Unit testing framework for backend logic
pytest-django>=4.7                  # Django integration for pytest

# Deployment & Production
gunicorn>=21.2                      # Production-grade WSGI server to handle client requests
whitenoise>=6.6                     # Serves static files efficiently in production

# Miscellaous
requests>=2.31                      # Used for calling external APIs for notifications/services